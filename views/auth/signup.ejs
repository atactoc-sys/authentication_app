<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIGN UP PAGE</title>
    <link rel="stylesheet" href="/css/signup.css">
</head>

<body class="dark-theme">
    <div class="container">
        <a id="back-link" class="backswitch" href="/">HOME</a>
        <h3 class="auth-title">Sign Up</h3>

        <form class="auth-form" action="/auth/signup" method="POST">
            <div class="form-group">
                <label class="form-label" for="name">Name</label>
                <input class="form-input" type="text" name="name" value="<%= typeof name !== 'undefined' ? name : '' %>"
                    required placeholder="please enter your name">
            </div>
            <div class="form-group">
                <label class="form-label" for="email">Email</label>
                <input class="form-input" type="email" name="email"
                    value="<%= typeof email !== 'undefined' ? email : '' %>" required placeholder="example@xyz.abc">
            </div>
            <div class="form-group">
                <label class="form-label" for="password">Password</label>
                <input class="form-input" type="password" name="password" required placeholder="********">
            </div>
            <div class="form-group">
                <label class="form-label" for="password2">Confirm Password</label>
                <input class="form-input" type="password" name="password2" required placeholder="********">
            </div>
            <button class="auth-button" type="submit">Sign Up</button>
        </form>
        <div class="auth-links">
            <a id="login-link" class="auth-link" href="/auth/login">Log In</a>
        </div>
        <% if (errors && errors.length> 0) { %>
            <ul class="error-list">
                <% errors.forEach(error=> { %>
                    <li class="error-message">
                        <%= error.msg %>
                    </li>
                    <% }) %>
            </ul>
            <% } %>
    </div>
</body>

</html>