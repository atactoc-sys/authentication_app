<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <link rel="stylesheet" href="/css/forgotpass.css">
</head>

<body class="dark-theme">
    <div class="container">
        <a href="/auth/login" id="home-link" class="auth-link">BACK</a>
        <h3 class="auth-title">Forgot Password</h3>
        <form action="/auth/forgot-password" method="POST" class="auth-form">
            <div class="form-group">
                <label for="email" class="form-label">Email:</label>
                <input type="email" id="email" name="email" class="form-input" required placeholder="example@xyz.abc">
            </div>
            <button type="submit" class="auth-button">Send Verification Link</button>
        </form>
        <% if (error) { %>
            <p class="error-message">
                <%= error %>
            </p>
            <% } %>
                <% if (success) { %>
                    <p class="success-message">
                        <%= success %>
                    </p>
                    <% } %>
    </div>
</body>

</html>